#pragma once

#include "lcpp/cli/dependencies/ezEngine.h"

namespace lcpp
{
    const char* const g_ApplicationTitle = "LISP in C++ CLI";

    // Should not contain whitespace
    const char* const g_ApplicationTitleShort = "lcppCLI";
}
